<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Upload your Excel files to create interactive dashboards">
    <title>Upload Excel - Excel Dashboard Generator</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        :root {
            --primary-dark: #1a1d20;
            --secondary-dark: #2c3034;
            --accent-color: #0d6efd;
            --border-color: rgba(255,255,255,0.1);
            --text-light: #f8f9fa;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--primary-dark);
            color: var(--text-light);
            min-height: 100vh;
        }

        /* Advertisement Styles */
        .ad-space {
            background: linear-gradient(to right, rgba(44, 48, 52, 0.8), rgba(44, 48, 52, 0.6));
            border: 1px solid var(--border-color);
            border-radius: 6px;
            padding: 10px;
            margin: 1.5rem 0;
            text-align: center;
            position: relative;
        }

        .ad-space::before {
            content: 'Advertisement Space';
            position: absolute;
            top: -10px;
            left: 50%;
            transform: translateX(-50%);
            background: var(--secondary-dark);
            padding: 0 8px;
            font-size: 0.75rem;
            color: #6c757d;
            letter-spacing: 0.5px;
            text-transform: uppercase;
        }

        .ad-space-horizontal {
            width: 100%;
            height: 100px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
    </style>
</head>
<body>
    <!-- Header will be loaded here -->
    <div id="header"></div>

    <main class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <!-- Top Advertisement Space -->
                <div class="ad-space ad-space-horizontal">
                    <!-- Ad Content Here -->
                </div>
                
                <div class="card shadow-lg">
                    // ...existing content...
                </div>
            </div>
        </div>
    </main>
</body>
</html>
